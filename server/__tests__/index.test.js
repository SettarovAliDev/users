describe('User signin endpoints', () => {
  it('shoould test index.js', () => {
    process.env = { NODE_ENV: 'production' };
    console.log(process.env.NODE_ENV);
    // const app = require('../index');
  });
});
